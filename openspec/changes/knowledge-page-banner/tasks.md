## 1. Rename `knowledge-menus` → `knowledge-content` (backend, mechanical)

- [x] 1.1 In `server/cmd/config/config.go`: rename `AppConfig.KnowledgeMenus` field to `KnowledgeContent` with `mapstructure:"knowledge-content"`; rename `GetKnowledgeMenusConfig()` to `GetKnowledgeContentConfig()`; update the doc comments referencing `[knowledge-menus]`.
- [x] 1.2 `git mv server/cmd/config/knowledge_menus_config_test.go server/cmd/config/knowledge_content_config_test.go`; update its `[knowledge-menus]` TOML fixtures to `[knowledge-content]`, its calls to `GetKnowledgeMenusConfig()` to `GetKnowledgeContentConfig()`, and test/function names and comments accordingly.
- [x] 1.3 `git mv server/api/kbhandler/kb_menu_labels.go server/api/kbhandler/kb_content_labels.go`; rename `LoadKnowledgeMenuLabels` → `LoadKnowledgeContentLabels`, `resolveKnowledgeMenuLabelsPath` → `resolveKnowledgeContentLabelsPath`, `rawKnowledgeMenuLabels` → `rawKnowledgeContentLabels`, env var `KNOWLEDGE_MENU_LABELS_DIR` → `KNOWLEDGE_CONTENT_LABELS_DIR`, and update the path it resolves to `config/knowledge-content/labels-<lang>.toml`. Also renamed `kb_menu_labels_test.go` → `kb_content_labels_test.go` and updated `kb_menu_handler_test.go`'s helper/field references (both discovered during implementation, not listed in the original task text).
- [x] 1.4 In `server/api/kbhandler/kb_menu_handler.go`: update calls to `appconfig.GetKnowledgeContentConfig()` and `LoadKnowledgeContentLabels(...)`; update doc comments referencing `[knowledge-menus]` / `config/knowledge-menus/`. Leave the handler function name `GetKbMenuConfig` and the route `/api/v1/kb/menu-config` unchanged (see design.md Decision 2).
- [x] 1.5 `git mv config/knowledge-menus config/knowledge-content` (both `labels-en.toml` and `labels-zh-cn.toml`).
- [x] 1.6 In `config.toml` and `config.local.toml`: rename the `[knowledge-menus]` section header to `[knowledge-content]`, keeping its existing `kb-*` entries unchanged. (`config.toml` had no such section; only `config.local.toml` did.)
- [x] 1.7 Update remaining code comments referencing `[knowledge-menus]` / `config/knowledge-menus/` in `web/src/lib/services/kbService.ts` and `web/src/routes/home3/knowledge/+page.svelte` to say `[knowledge-content]` / `config/knowledge-content/`. Also fixed a stray comment in `server/api/sitehandler/workspace_content_labels.go` and in `config/knowledge-content/labels-en.toml`.
- [x] 1.8 Ran `cd server && go build ./... && go test ./cmd/config/... ./api/kbhandler/...` — build succeeds; all rename-related tests pass. Some pre-existing unrelated failures in `search_registry_test.go` (ParadeDB/registry search subsystem) are unaffected by this change.

## 2. Add `[knowledge]` site-config section (backend + frontend types)

- [x] 2.1 In `server/api/sitehandler/sitehandler.go`: add a `Knowledge` struct (`Kicker`, `BannerTitle`, `BannerSubtitle`, `BannerImage` — mirroring `Workspace` minus `Apps`) and add `Knowledge Knowledge \`toml:"knowledge" json:"knowledge"\`` to `SiteConfig`.
- [x] 2.2 Add a `[knowledge]` section to `config/site/site-default.toml` and `config/site/site-default-zh-cn.toml` (both carry their own `[workspace]` copy) — `kicker`/`banner_title`/`banner_subtitle` describing browsing/managing the knowledge base, `banner_image = "/images/kb-documents.jpg"` (an existing asset already used elsewhere in site-default.toml for knowledge-related sections, more fitting than reusing the workspace's `angleWalls.jpg`).
- [x] 2.3 In `web/src/lib/services/siteConfigService.ts`: add a `SiteKnowledge` interface (`kicker`, `banner_title`, `banner_subtitle`, `banner_image`) and add `knowledge: SiteKnowledge` to the `SiteConfig` interface.
- [x] 2.4 Ran `cd server && go build ./... && go test ./api/sitehandler/...` — build and tests pass.

## 3. Add banner content ids to `knowledge-content` labels

- [x] 3.1 Added `kb-kicker`, `kb-banner-title`, `kb-banner-subtitle` entries to the `[labels]` table in `config/knowledge-content/labels-zh-cn.toml`, with Knowledge-appropriate Chinese copy (not a copy of the workspace text).
- [x] 3.2 Confirmed `labels-en.toml`'s `[labels]` table is empty by design (English menu labels come from hardcoded defaults in `+page.svelte`) — English banner defaults now come from `site-default.toml`'s new `[knowledge]` section, so no entry needed there. Updated its header comment to mention the new masthead ids.

## 4. Masthead banner UI on `/home3/knowledge`

- [x] 4.1 No `+page.ts`/`data` prop reintroduced. `+page.svelte` already imports `page` from `$app/state` (used for search params); confirmed SvelteKit merges the route's `+layout.ts` load into `page.data`, so `page.data.siteConfig` (and `.branding.site_name`) is available directly — added `kbSiteConfig = $derived(page.data.siteConfig?.knowledge)` and `siteName = $derived(page.data.siteConfig?.branding?.site_name ?? '')`. `getKbMenuConfig()`'s existing `labels` map is reused for `kb-kicker`/`kb-banner-title`/`kb-banner-subtitle` (added those 3 ids to `knownMenuIds` so they don't trip the existing "unrecognized id" diagnostic).
- [x] 4.2 Added the masthead section (image + gradient overlay, diamond watermark scaled down, kicker + dateline rule, title, subtitle), structurally matching the workspace masthead but without the announcements bulletin, tenant-error line, or a separate diagnostics block — the page's existing unknown-id diagnostic already covers the new banner ids since they're in `knownMenuIds`. Padding/type scale reduced (`py-8 md:py-10`, smaller title clamp) since this is a dense app page, not a scrolling marketing page.
- [x] 4.3 Wired kicker/title/subtitle to `menuLabels['kb-kicker' | 'kb-banner-title' | 'kb-banner-subtitle'] ?? kbSiteConfig?.<field> ?? ''`, mirroring the workspace page's `contentLabels[...] ?? cfg.workspace.<field>` pattern. `bannerImage` comes from `kbSiteConfig?.banner_image` (no per-locale override needed for an image path).
- [x] 4.4 Added `<svelte:head><title>{bannerTitleText}{siteName ? ' — ' + siteName : ''}</title></svelte:head>`.
- [x] 4.5 Wrapped the masthead + `.kb-page` in an outer `flex flex-col` div capped at `height: calc(100vh - 4.25rem)`; masthead is `flex-shrink-0` (natural height), `.kb-page` is `flex-1` with `min-height: 0` (was previously its own `height: calc(100vh - 4.25rem)`, now removed) so it fills the remaining space regardless of the masthead's actual rendered height. Verified no overflow (see 5.2).

## 5. Verification

- [x] 5.1 Verified via the already-running dev server + Playwright screenshot on `/home3/knowledge`: `SiteHeader` renders, masthead renders below it with Knowledge-specific copy ("Your Knowledge Base" / "Browse, search, and manage every document and artifact in one place." — not "我的工作台"), no announcements strip, sidebar starts directly with "Knowledge" header (no redundant branding row).
- [x] 5.2 Verified no overflow: `document.documentElement.scrollHeight` (901px) ≈ `window.innerHeight` (900px, 1px rounding) at 1600×900 — header + masthead + kb-page fit exactly via the new flex-column wrapper, no extra scrollbar.
- [x] 5.3 Switched locale via the header's language switcher. The nav labels, dateline, and `<title>` all correctly reactively updated between zh-cn/en. The masthead kicker/title/subtitle, however, did **not** re-fetch on locale switch — traced this to `page.data.siteConfig` being fetched once by `+layout.ts` and not re-run on a client-side locale change (no `invalidate()`/dependency on `getLocale()`). Confirmed by reproducing the identical behavior on `/semos/workspace` today (its "工作台" kicker also stays put after switching to English) — this is a pre-existing limitation of the shared site-config fetch, not a regression introduced by this change, and matches design.md's non-goal of not fixing unrelated pre-existing issues. The per-locale `knowledge-content/labels-<lang>.toml` override path itself (`kb-kicker` etc.) is wired correctly and would apply once authenticated (see 5.1's screenshot showing the site-default zh-cn kicker already differing correctly from the English default at initial load).
- [x] 5.4 Toggled dark mode via Playwright — masthead gradient/overlay/watermark/text colors all adapt correctly, matching the workspace masthead's dark-mode treatment (same hardcoded `#faf9f7`/`#101216`/`#b08d57` Tailwind `dark:` classes).
- [x] 5.5 Reloaded `/semos/workspace` (full-page screenshot) — masthead, announcements ("暂无公告。"), apps section, footer all identical to before this change.
- [x] 5.6 Ran `cd server && go build ./... && go vet ./...` (clean) and `cd web && npx svelte-check` (0 errors, 23 pre-existing warnings unrelated to this change).

## 6. Rollback: banner removed per user feedback

After visual review, the user judged the masthead banner did not look good on `/home3/knowledge` and asked for its removal. The `SiteHeader` (group 4.1's header-sharing) and the `knowledge-menus`→`knowledge-content` rename (group 1) were kept — only the banner and its purpose-built config plumbing came out.

- [x] 6.1 `+page.svelte`: removed the masthead `<section>` markup, the outer flex-column wrapper (restored `.kb-page`'s own `height: calc(100vh - 4.25rem)`), the `.reveal`/`.is-visible` CSS, the `<svelte:head><title>`, and all banner-only script state (`KB_KICKER_ID`/`KB_BANNER_TITLE_ID`/`KB_BANNER_SUBTITLE_ID`, `kbSiteConfig`, `bannerKickerText`/`bannerTitleText`/`bannerSubtitleText`/`bannerImage`, `titleLines`, `dateline`, the `reveal()` action, `siteName`). Reverted `knownMenuIds` back to menu-ids-only (dropped the 3 banner ids it had absorbed).
- [x] 6.2 Removed the `Knowledge` Go struct and its `SiteConfig.Knowledge` field (`server/api/sitehandler/sitehandler.go`), the `SiteKnowledge` TS interface and `SiteConfig.knowledge` field (`web/src/lib/services/siteConfigService.ts`), and the `[knowledge]` section from both `config/site/site-default.toml` and `site-default-zh-cn.toml` — this plumbing existed only to feed the banner, so it was deleted rather than left dormant.
- [x] 6.3 Removed the `kb-kicker`/`kb-banner-title`/`kb-banner-subtitle` entries from `config/knowledge-content/labels-zh-cn.toml` and reverted the header-comment changes in both `labels-zh-cn.toml` and `labels-en.toml` that had mentioned the masthead.
- [x] 6.4 Deleted `specs/knowledge-page-banner/spec.md` — the capability never reached archived/established status, so it's dropped rather than recorded as a REMOVED delta (that treatment is for capabilities that were once true and shipped; this one shipped and un-shipped within the same unarchived change).
- [x] 6.5 Re-ran `cd server && go build ./...` and `cd web && npx svelte-check` (both clean) and visually re-verified `/home3/knowledge` in the browser: header present, no banner, sidebar unchanged, no layout overflow (`scrollHeight` 901 ≈ `innerHeight` 900), no new console errors.
