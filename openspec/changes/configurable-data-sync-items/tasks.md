## 1. Database migrations

- [x] 1.1 Use the `db-migration` skill to add a goose migration creating `kb.data_sync_items` (`id TEXT PRIMARY KEY`, `kind TEXT NOT NULL DEFAULT 'table' CHECK (kind IN ('table','table_with_files'))`, `origin TEXT NOT NULL CHECK (origin IN ('local','learned'))`, `table_name TEXT NOT NULL`, `cursor_col TEXT NOT NULL`, `natural_key JSONB NOT NULL`, `columns JSONB NOT NULL`, `json_columns JSONB NOT NULL DEFAULT '[]'`, `filter TEXT`, `file_column TEXT`, `file_dir_env TEXT`, `file_dir_default_subdir TEXT`, `created_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`, `created_by TEXT`, `updated_at TIMESTAMPTZ NOT NULL DEFAULT NOW()`).
- [x] 1.2 Use the `db-migration` skill to add a goose migration adding `update_time TIMESTAMPTZ` to `kb.videos`, backfilling it from `created_at`, attaching the existing reusable `kb.set_update_time()` trigger (`BEFORE UPDATE`), and adding `UNIQUE (stored_path)`.
- [x] 1.3 Confirm both migrations apply cleanly via `mise dev`'s auto-migrate; check `project_db_migration` for the new version ids.

## 2. Registry: kinds, file config, DB-backed items

- [x] 2.1 In `server/api/datasync/registry.go`, add `SyncKind` (`KindTable = ""`/`"table"`, `KindTableWithFiles = "table_with_files"`), `SyncOrigin` (`OriginLocal = "local"`, `OriginLearned = "learned"`), and extend `TableSyncItem` with `Kind SyncKind`, `Origin SyncOrigin`, `FileColumn string`, `FileDirEnv string`, `FileDirDefaultSubdir string`. Confirm `kb_product_names`'s existing literal still compiles unchanged (zero-value `Kind`/`Origin` behave as compiled defaults).
- [x] 2.2 Add `dbItemByID(ctx, db, id) (TableSyncItem, bool, error)` and `dbListItems(ctx, db) ([]TableSyncItem, error)` reading/decoding `kb.data_sync_items` (JSONB columns) into `TableSyncItem`.
- [x] 2.3 Add `ResolveItem(ctx, db, id) (TableSyncItem, bool, error)`: checks compiled `Registry` first, falls back to `dbItemByID`. Update `pull_handler.go`, `admin_handler.go`'s preview/apply/edit/delete, and the new file/discovery handlers to use it instead of the old synchronous `ItemByID` (keep `ItemByID` for the compiled-only case if anything still needs it, e.g. discovery's own "what do I compile in" list).
- [x] 2.4 Add `insertItem(ctx, db, item TableSyncItem, createdBy string) error` (always `origin='local'`), rejecting (at the Go layer, ahead of any DB constraint) an id that collides with `Registry` or an existing `kb.data_sync_items` row. Add `upsertLearnedItem(ctx, db, item TableSyncItem) error` used only by discovery caching (§4.3), always writing `origin='learned'`, overwriting an existing `learned` row's shape but refusing to overwrite a `local` one (a target's own locally-authored item always wins over a same-id item a source happens to advertise).
- [x] 2.5 Add `updateItem(ctx, db, id string, item TableSyncItem) error`: only succeeds when the existing row's `origin='local'`; replaces its shape (`id`/`origin`/`created_at`/`created_by` unchanged) and clears `kb.data_sync_state` for that id in the same transaction (design.md Decision 6). Add `deleteItem(ctx, db, id string) error`: works for `origin` `local` or `learned`; also deletes the `kb.data_sync_state` row for that id; errors if the id resolves only to a compiled item.
- [x] 2.6 Add identifier validation per design.md Decision 5: `validateTable`, `validateIdentifier` (for columns/cursor/natural-key/file-column entries), `validateKind`; share it between the create (§4.1) and edit (§4.7) handlers.

## 3. Source side: file transfer + item-definitions discovery

- [x] 3.1 Implement `GET /api/internal/data-sync/items/:itemId/files?key=<url-encoded JSON array>` in `pull_handler.go`: same shared-secret auth as `.../changes`; resolve the item via `ResolveItem`; 400 if its `Kind != KindTableWithFiles`; decode `key` into the natural-key values (in `NaturalKey` order); run `SELECT <FileColumn> FROM <Table> WHERE [<Filter> AND] (<NaturalKey...>) = (<key values>)`; 404 if no row or the column is empty; 404 if `os.Stat` on the path fails; otherwise stream the file with `io.Copy` and `Content-Disposition: attachment; filename="<basename>"`.
- [x] 3.2 Implement `GET /api/internal/data-sync/items` in `pull_handler.go`: same shared-secret auth; return every item this instance can serve (`Registry` ∪ its own `dbListItems`) as JSON definitions (id, kind, table, cursor_col, natural_key, columns, json_columns, filter, file_column, file_dir_env, file_dir_default_subdir).
- [x] 3.3 Register both routes in `server/api/routes.go` next to the existing `.../changes` route (same ungated, pre-`apiGroup` placement).

## 4. Target side: create/edit/delete endpoints, discovery caching, file materialization

- [x] 4.1 Implement `POST /api/v1/data-sync/items` in `admin_handler.go`: parse+validate the request body (kind, table, cursor_col, natural_key, columns, json_columns, filter, and — for `table_with_files` — file_column + file_dir_env/file_dir_default_subdir); call `insertItem`; return the created item view or a 4xx with the validation error.
- [x] 4.2 Add `fetchSourceItemDefinitions(ctx, sourceURL, secret) ([]TableSyncItem, error)` in `client.go` calling the new `GET .../items` discovery endpoint.
- [x] 4.3 In `HandleListSyncItems`, best-effort call `fetchSourceItemDefinitions` when source config is present (log and continue on failure, don't fail the list); for each returned definition, `upsertLearnedItem` it (create-or-refresh the local `origin='learned'` cache per design.md Decisions 4 & 6); then list `Registry` ∪ local `kb.data_sync_items` as today, with each view's `origin` set to `compiled`/`local`/`learned`.
- [x] 4.4 Add `resolveFileDir(item TableSyncItem) (string, error)` in a new `files.go`: `os.Getenv(item.FileDirEnv)` if set, else `<DATA_HOME_DIR>/<item.FileDirDefaultSubdir>`, mirroring `videohandler.videoDir()`'s resolution order; error (fail closed) if neither resolves to a non-empty path.
- [x] 4.5 Add `materializeFiles(ctx, item, sourceURL, secret, rows []Row) error` in `files.go`: for each row with a non-empty `FileColumn` value, call the new per-file endpoint (keyed by that row's natural key), `os.MkdirAll` the resolved dir, write the bytes under a fresh unique name (mirror `UploadVideo`'s `<UnixNano>_<sanitized-basename>` pattern), and overwrite `row[item.FileColumn]` with the new local path. Skip rows with an empty file column.
- [x] 4.6 In `HandleApplySync`, after `fetchAllChanges` and before `upsertRows`, call `materializeFiles` when `item.Kind == KindTableWithFiles`; on any file-fetch/write error, behave exactly like an upsert error today (record `last_error`, do not advance the cursor).
- [x] 4.7 Implement `PUT /api/v1/data-sync/items/:itemId` in `admin_handler.go`: resolve the id; 404 if unknown; 400 with a clear message if it's compiled or `origin='learned'` (design.md Decision 6); otherwise validate the submitted shape (reusing §2.6) and call `updateItem`; return the updated item view.
- [x] 4.8 Implement `DELETE /api/v1/data-sync/items/:itemId` in `admin_handler.go`: resolve the id; 404 if unknown; 400 with a clear message if it's compiled; otherwise call `deleteItem`; return `{ok: true}`.
- [x] 4.9 Register the two new routes under `apiGroup` in `server/api/routes.go`, alongside the existing three.

## 5. Admin UI

- [x] 5.1 Add a "New Data Syncher" button to `sync-data-view.svelte`'s toolbar, opening a form (kind selector; shared fields: id, table, cursor column, natural key, columns, JSON columns, filter; file-only fields shown when kind is `table_with_files`: file column, file dir env var, file dir default subdir). The same form, pre-filled, is reused for editing.
- [x] 5.2 Add `createSyncItem(...)`, `updateSyncItem(...)`, `deleteSyncItem(...)` to `sync-data-client.ts` calling the new `POST`/`PUT`/`DELETE` endpoints; on success, close the form/dialog and refresh the list (reusing `loadItems`); surface validation errors in the existing error-banner style.
- [x] 5.3 Show each row's origin (compiled/local/learned) and gate the row actions accordingly: compiled → Preview/Sync only; learned → Preview/Sync/Delete; local → Preview/Sync/Edit/Delete. Delete asks for confirmation before calling `deleteSyncItem`.

## 6. Registering kb.videos as the proof item

- [ ] 6.1 After the `kb.videos` migration (§1.2) lands, use the new "New Data Syncher" UI on the Mac (source) to create the `kb_videos` item: table `kb.videos`, cursor `update_time`, natural key `(stored_path)`, columns = every `kb.videos` column except `id`, kind `table_with_files`, file column `stored_path`, file dir env `VIDEO_DIR`, file dir default subdir `Videos`.

## 7. Local verification

- [x] 7.1 Unit-test identifier validation (§2.6): valid/invalid table names, column names, missing-file-column-for-table_with_files, duplicate id against both `Registry` and an existing DB item.
- [x] 7.2 Unit-test `materializeFiles`: row with a file gets copied and its column rewritten; row with an empty file column is left untouched and no fetch is attempted; a source file-fetch failure surfaces as an error without upserting that page.
- [x] 7.3 Exercise discovery end-to-end locally (mirroring `production-data-sync`'s own `integration_test.go` pattern, `TEST_DATABASE_URL`-guarded): create an item via `insertItem` against a "source" scratch DB, confirm a "target" scratch DB's list call learns and caches it (`origin='learned'`), and that a subsequent preview/apply for that id succeeds using only the cached definition (source discovery call stubbed unreachable). Also confirm a second list call picks up an edited source shape (upsert, not insert-only).
- [x] 7.4 Exercise a `table_with_files` apply end-to-end against local scratch tables/dirs: seed a source-side file + row, apply, confirm the target-side file exists under the resolved target dir and the upserted row's file column points at it.
- [x] 7.5 Unit-test edit/delete gating (§4.7, §4.8): editing/deleting a compiled id is rejected; editing a `learned` id is rejected but deleting it succeeds and only removes the local cache row; editing/deleting a `local` id succeeds and, for edit, clears its `kb.data_sync_state` row; deleting a `local` id also removes its `kb.data_sync_state` row.
- [x] 7.6 `cd ChenWeb && mise run build-server-linux` still succeeds with the new code.

## 8. Documentation — devdoc as source of truth

- [x] 8.1 Once §§1-7 are implemented and locally verified, rewrite `KnowledgeStore/doc-repo/devdocs/202609/2026091601-devdoc-data-sync.md` as the single source-of-truth operational document for data sync: setup/usage (already there, keep current), plus the registry model (compiled vs. `local` vs. `learned`, and what that means for who can edit/delete what), both item kinds (`table`, `table_with_files`) with the file-copy-then-row-upsert mechanics, the discovery/caching behavior between a source and a target, and an updated troubleshooting table covering the new failure modes (missing source file, invalid identifiers rejected on create/edit, editing a `learned`/compiled item rejected, discovery unreachable). Reference `openspec/changes/production-data-sync/` and `openspec/changes/configurable-data-sync-items/` for design rationale instead of re-deriving it inline. This is the last task — it documents the as-built system, not the proposal.
