## 1. Migration

- [x] 1.1 Add a goose migration (`project_migrations/`, next timestamp after `20260811000004`) adding `identity_key TEXT` (nullable) to `kb.ontology_candidates`, following the `ALTER TABLE` pattern in `20260731000019_add_superseded_to_ontology_candidates.sql`. Use the `db-migration` skill for this step. (`20260811000005_add_identity_key_to_ontology_candidates.sql`; the real migrations directory for this project is `project_migrations/` at the repo root, not the generic skill's `server/migrations/` — confirmed against existing files before writing.)
- [x] 1.2 In the same migration, add a plain (non-unique) btree index on `identity_key`.
- [x] 1.3 Confirm the migration applies cleanly against the dev DB (`mise dev`/air auto-applies on save — check `SELECT * FROM project_db_migration ORDER BY id DESC LIMIT 5`) and write a `-- +goose Down` that drops the index and column. Verified live: `project_db_migration` row 251 shows `20260811000005` applied at 2026-08-11 18:17:06, and `\d kb.ontology_candidates` confirms both `identity_key text` and `idx_kb_ontology_candidates_identity_key` btree exist on the dev DB.

## 2. Identity key computation

- [x] 2.1 Add `server/api/ontology/candidates/identity.go` with `IdentityKey(candidateKind, moduleID string, payload []byte) string`, implementing design.md Decision 1: returns `""` unless `candidateKind == "term"`; unmarshal `label` (string) and `aliases` ([]string, optional); build the normalized (lowercase, trimmed) deduped sorted set `{label} ∪ aliases`; if empty, return `""`; else `sha256(moduleID + "\x00" + termKind + "\x00" + strings.Join(sortedSet, "\x1e"))` hex-encoded, where `termKind` is the payload's `term_kind` field (empty string if absent).
- [x] 2.2 Unit test `IdentityKey`: label/alias-swap case from the bug doc produces the same key both ways; different `term_kind` with the same label produces different keys; non-`term` candidate kinds and empty-label payloads return `""`; malformed/non-JSON payload does not panic (mirror `canonicalJSON`'s error handling — return `""` on unmarshal failure, matching `validateCandidate`'s existing tolerance). (`identity_test.go`, 7 tests)

## 3. Wire into CreateCandidate

- [x] 3.1 In `CreateCandidate` (`candidates_store.go:141-207`), compute `identity_key` via `IdentityKey(c.CandidateKind, c.ProposedModuleID, c.ProposedPayload)` alongside the existing `Fingerprint` call, and add it to the INSERT statement's column list/values (nullable — use the existing `nullableString` helper).
- [x] 3.2 Add `IdentityKey` to `candidateColumns`/`scanCandidate` so it round-trips on `GetCandidate`/`ListCandidates` reads, and to the `Candidate` struct as `IdentityKey string`.
- [x] 3.3 After a successful insert where `Reused == false` and the computed identity key is non-empty, run the match query from design.md Decision 3 (`WHERE identity_key = $1 AND id != $2 AND status NOT IN ('rejected', 'superseded')`) and, for each match, append a `{"match_type": "candidate", "candidate_id": ..., "matched_on": "identity_key", "detected_at": ...}` entry to `candidate_matches` on both the new row and the matched row via a single `UPDATE ... SET candidate_matches = COALESCE(candidate_matches, '[]'::jsonb) || $entry::jsonb` statement each. (`matchByIdentityKey`, `candidates_store.go`)
- [x] 3.4 Return the newly created row with its own `candidate_matches` reflecting any matches found during this call (re-fetch or apply the same update locally before returning, so the caller doesn't have to re-query to see it).

## 4. Tests

- [x] 4.1 `CreateCandidate` test: two candidates with swapped label/alias (the bug doc's reproduction case) get different `fingerprint`s but the same `identity_key`, and each ends up with a `candidate_matches` entry referencing the other. (`TestCreateCandidateMatchesSwappedLabelAlias`)
- [x] 4.2 `CreateCandidate` test: a third candidate with a different `term_kind` but the same label as an existing one does NOT get matched (different `identity_key`). (`TestCreateCandidateDoesNotMatchAcrossTermKinds`)
- [x] 4.3 `CreateCandidate` test: matching only against `rejected`/`superseded` rows produces no match entries. (`TestCreateCandidateIdentityMatchQueryExcludesTerminalStatuses` — locks in the query's `status NOT IN (...)` clause and confirms an empty result produces no UPDATE; sqlmock can't simulate Postgres's own status filtering, so the terminal-status exclusion itself relies on the SQL text, not a mocked DB row.)
- [x] 4.4 `CreateCandidate` test: a fingerprint-reuse call (`Reused == true`) does not run identity-key matching or touch `candidate_matches`. (`TestCreateCandidateFingerprintReuseSkipsIdentityMatching`)
- [x] 4.5 `CreateCandidate` test: candidate_kind other than `term` (e.g. `axiom`) never gets an `identity_key` and is never matched, even if two axiom payloads happen to be textually similar. (`TestCreateCandidateNonTermCandidateKindNeverMatched`)
- [x] 4.6 Confirm existing `candidates_store_test.go`/`p2_exit_test.go`/`state_machine_test.go`/`promote_test.go` suites still pass. Not literally unmodified as originally worded: `candidateColumns` gained a column, so every sqlmock fixture row (`candidateRow`/`AddRow`) and the two `CreateCandidate` `WithArgs` call sites needed a mechanical extra `nil`/`identity_key` argument appended — same assertions, same behavior, all still passing. `state_machine_test.go` needed no changes (pure function, no DB shape). Full package suite: `go test ./...` passes.

## 5. Docs

- [x] 5.1 Update `KnowledgeStore/doc-repo/bugs/202608/2026081101-bug-ontology-candidates-fingerprint-dedup.md`'s "Still owed" section to reference this shipped change, and move its `OPEN.md` entry to resolved once merged and verified. Status moved to `fixed-unverified` (not fully `resolved` — the doc's own status vocabulary reserves that for `fixed-verified`/`wontfix`; live end-to-end verification is still outstanding, tracked in the bug doc's own "Still owed").
- [ ] 5.2 Check whether `KnowledgeStore/doc-repo/user-manuals/doc-processsors.md` §9 (candidate/glossary description) needs a line noting that near-duplicate candidates are now soft-flagged to each other via `candidate_matches` — only if that manual is considered current/maintained at merge time (it has unrelated uncommitted scratch content as of 2026-08-11; coordinate with whoever owns that file before editing it). Deliberately left untouched this session per that same caveat — the file's owner should fold this in alongside resolving its existing uncommitted content, not have a second uncoordinated edit land on top.
