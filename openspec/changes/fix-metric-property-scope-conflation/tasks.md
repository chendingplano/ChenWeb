## 1. ClassSynthesizer seam in `assertions`

- [x] 1.1 Add `server/api/ontology/assertions/class_synthesizer_registry.go`: `ClassSynthesisInput`, `ClassSynthesizer` func type, `RegisterClassSynthesizer`, `SynthesizeClass` — mirroring `normalizer_registry.go`'s registry shape, using the existing `DBX` interface (`assertions_store.go:83`) so callers can pass `*sql.Tx`.
- [x] 1.2 Unit test: registering two synthesizers is a programming error (mirrors `RegisterNormalizer`'s existing-family check); `SynthesizeClass` with nothing registered returns a clear error rather than a nil-pointer panic.

## 2. Extract `EnsureAcceptedOrCreate`'s core into a tx-scoped helper

- [x] 2.1 In `server/api/ontology/keywords/alignment.go`, extract the body of `EnsureAcceptedOrCreate`'s `withKeywordIdentityMutation` closure (existing-alignment check, `terms.TermStore.CreateTerm`, label creation, alias creation, alignment-assertion write, decision-log append) into an unexported helper taking a tx-scoped `txStore`/`db` directly, with no transaction-opening of its own.
- [x] 2.2 `EnsureAcceptedOrCreate` becomes a thin wrapper: open `withKeywordIdentityMutation`, call the new helper, return its result. Confirm its exported signature and behavior are unchanged.
- [x] 2.3 Run `go test ./server/api/ontology/keywords/...` and confirm `alignment_test.go` passes unchanged — proves the refactor preserves behavior. (3 pre-existing failures confirmed identical before/after via git stash: a stale sqlmock arg-count expectation in `TestAlignmentsStoreEnsureAcceptedOrCreateAutoCreatesTerm`, and two env-var-order-dependent resolver-mode tests — none related to this refactor.)
- [x] 2.4 Add `keywords`' `init()` registration: `assertions.RegisterClassSynthesizer(...)` calling the extracted helper. When `ClassSynthesisInput.ConceptID != ""`, run the same existing-alignment-check + create + align logic the helper provides; when empty, call `terms.TermStore{DB: db}.CreateTerm` directly with no alignment step. (New file `keywords/class_synthesis.go`; `ensureAcceptedOrCreate` also now returns a `created bool` so the synthesizer can report it.)
- [x] 2.5 Unit test the registered synthesizer directly (`keywords/class_synthesis_test.go`): no-`ConceptID` creates a term with no transaction/alignment write; an already-aligned concept reuses the existing term with no writes at all; a concept with no existing alignment creates the term+labels then hands off to `ensureAccepted`'s already-tested alignment-write logic. (Rollback-on-tx-failure isn't separately tested — `synthesizeClass` opens no transaction of its own, so that guarantee is exercised at the metric_lossless_writer.go integration level, task 3.4.)

## 3. Wire the synthesizer into `metric_lossless_writer.go`

- [x] 3.1 Rewrote `resolveOrCreateMetricClass` to call `SynthesizeClass` directly instead of `classfoundation.ContractStore.CreateIdentityOnlyClass`, passing a `ClassSynthesisInput` built from `metricCandidatePayload`'s new `ConceptID`/`Definition`/`ValueDataType`/`ValueRangeTypeText`/`ExtraProperties` fields plus the already-resolved `unitTermID`. The header-existence pre-check on `candidateTermID` now only runs when `ConceptID == ""` (the concept-based branch has its own idempotent check inside `ensureAcceptedOrCreate`, and checking headers for the hash-derived candidate would be checking the wrong identity once a concept is available).
- [x] 3.2 `classIdentityState` (`ClassResolvedExisting`/`ClassProvisionalNew`) now driven by `SynthesizeClass`'s `created` return value.
- [x] 3.3 Reviewed `metric_lossless_writer_integration_test.go`: its existing tests construct `metricCandidatePayload` with no `ConceptID`, so they exercise the no-concept branch unchanged (same hash-derived term_id, same `kb.ontology_term_headers` counts) — no edits needed there. **Not run in this environment** (`TEST_DATABASE_URL` unset, so these integration tests skip); reviewed by inspection only.
- [x] 3.4 Added `TestIntegrationWriteMetricLosslessProvisionalClassIsCatalogVisible`: a `ConceptID`-bearing metric occurrence's provisional class is asserted to be a real, queryable `kb.ontology_terms_current` row. Compiles and vets cleanly but is **unverified** in this environment for the same `TEST_DATABASE_URL` reason — run it before merging.

## 4. `extract-metrics.go` stops creating terms; `metric_normalizer.go` forwards concept + qualifier fields

- [x] 4.1 In `extract-metrics.go`'s `resolveAll`, remove the `s.Alignments.EnsureAcceptedOrCreate` call and the `res.TermID`/`res.Status = names.StatusTermResolved` assignment that followed it. Keep `names.ResolveAndObserve` and `keyword_concept_id` bookkeeping unchanged. (Also removed the now-dead `termAligner` interface/`Alignments` field, `parseOntologyTermPropertyMap`/`appconfig` usage in this file, and the 6 obsolete auto-promotion tests plus `scriptedTermAligner` from `extract-metrics_test.go` — they tested a call site that no longer exists; equivalent coverage moves to metric_lossless_writer_test.go, task 3.x.)
- [x] 4.2 Confirmed: `res.Status == names.StatusTermResolved` is now only ever true when the resolver itself already found a pre-existing accepted alignment (auto-promotion no longer sets it after the fact), so `metric_definition_term_id` stays unset for a first-seen concept. Already covered by the existing, passing `TestResolvingMetricsStoreLexicalSetsConceptOnly` (`extract-metrics_test.go:2122`), which asserts `metric_definition_term_id` is absent for a lexical-only (first-seen) resolution.
- [x] 4.3 Added `metric_desc`, `keyword_concept_id`, `metric_subject`, `reasoning_tags`, `ext_info`, `metric_categories`, `value_data_type` to `metricRow`/SQL (`value_class`, `metric_value`, `value_min`, `value_max`, `condition` were already selected). New `metricFieldMap` helper builds the dotted-path-capable field map both property maps resolve against.
- [x] 4.4 `metricCandidatePayloadForRow` adds `keyword_concept_id`, `definition` (metric_desc-first/formula_or_definition-fallback), `value_data_type`, `value_range_type_text`, `class_properties`, and `qualifiers` to the payload map.
- [x] 4.5 Updated `extract-metrics_test.go` (removed the 6 obsolete auto-promotion tests + `scriptedTermAligner`, `termAligner`/`keywords`/`appconfig` now-unused imports) and added 6 new tests to `metric_normalizer_test.go` covering definition sourcing, `keyword_concept_id` forwarding, and both property maps (see 5.6).

## 5. Generalize the property-map machinery; add `[semantic_assertion_property_map]`

- [x] 5.1 Moved (not just renamed) the generic helpers from `doc-processing/ontology_term_property_map.go` to `assertions/property_map.go` as exported `PropertyMapping`/`ParsePropertyMap`/`BuildMappedProperties` — necessary, not just tidiness: `doc-processing` already imports `assertions`, and the new `metric_normalizer.go` call site (task 5.3) lives in `assertions`, which cannot import `doc-processing` back. Old file deleted; `extract-metrics.go`'s class-level property-map call site moves to `metric_lossless_writer.go` (task 3.1), since that's where class synthesis now happens.
- [x] 5.2 Added `SemanticAssertionPropertyMapConfig`/`AppConfigDef.SemanticAssertionPropertyMap`/`GetSemanticAssertionPropertyMap()` mirroring the existing `OntologyTermPropertyMapConfig`.
- [x] 5.3 `metric_normalizer.go` builds `class_properties` and `qualifiers` sub-objects on the candidate payload via `BuildMappedProperties` against `[ontology_term_property_map]`/`[semantic_assertion_property_map]`'s `metric` mappings (parsed once per `Normalize` batch, not per row) and the row's field map.
- [x] 5.4 Added `ExtraProperties`/`Qualifiers map[string]any` plus `ConceptID`/`Definition`/`ValueDataType`/`ValueRangeTypeText` to `metricCandidatePayload` in `associate_semantics.go`, unmarshaled from the payload keys added in 5.3/4.4.
- [x] 5.5 Removed `metricQualifiers`. `processMetric` and `writeMetricLossless` both marshal `p.Qualifiers` directly into `Assertion.Qualifiers`.
- [x] 5.6 Removed the obsolete `TestMetricQualifiersCarryMetricDefinitionTermID` (tested the removed hardcoded function). Added `TestMetricCandidatePayloadForRowAppliesClassPropertyMap`/`AppliesQualifierMap` in `metric_normalizer_test.go` asserting on the configured-map-driven output instead.

## 6. Config changes

- [x] 6.1 `[ontology_term_property_map]`'s metric mapping in `config.local.toml` (`config.toml` has no such section) is now `[]`. Removed the 10 instance-level entries from the original review, plus `value_data_type`/`value_range_type`/`formula_or_definition` (duplicate the fixed synthesis's `value_type`/`range_type`/`Definition`), plus two more found while auditing the full original 16-entry list against design.md's "nothing beyond fixed synthesis fields" decision: `metric_unit` (duplicates `raw_unit`/`permitted_unit_term_ids`) and `metric_name` (duplicates the term's own prefLabel). Section left wired with an empty list for future class-level fields.
- [x] 6.2 Added `[semantic_assertion_property_map]` with 12 entries: the 10 originally-instance-level fields, plus `metric_name` (per D8 — the one hardcoded qualifier field worth keeping, now config-driven) and `value_class` (also occurrence-scoped: which value-class this specific occurrence represents, not addressed in the original task list — added on review since it was one of the 3 unaccounted-for entries in the original 16). `metric_unit` was not added here either — already covered by `unit_term_id`/`object_literal.unit` on the assertion itself.

## 7. Verification

- [x] 7.1 Confirmed `shared/go` was not touched (all changes are within `ChenWeb/`) — `go work sync` not needed.
- [x] 7.2 `go build ./...`, `go vet ./...`, `go test ./...` all run clean. Full failure catalogue (kbhandler, llmusage, keywords, names, seed, qudt-import — all pre-existing sqlmock/query staleness, confirmed via `git diff --stat` that none of those files were touched by this change) is unchanged before/after.
- [x] 7.3 Audited all 4 production readers of `metric_definition_term_id` outside `assertions`/`doc-processing`: `metrics_handler.go` (nullable `*string` API field), `metric_ontology_analysis_handler.go` (`LEFT JOIN` + `COALESCE(..., 'Not governed')`), `metric_adapter.go` (`coalesce(..., '')` in a diagnostic shadow-report query), `metrics_merge.go` (merge preserves whatever's already there). All four already handle an empty/NULL value gracefully — none assume eager post-extraction population. Consequence is timing-only: a metric now shows ungoverned until Phase D/`associate_semantics` runs, which is the intended behavior (finding 5), not a bug.
- [ ] 7.4 **Not done in this environment** (`TEST_DATABASE_URL` unset, no live Postgres available). `TestIntegrationWriteMetricLosslessProvisionalClassIsCatalogVisible` (task 3.4) covers exactly this end-to-end check and just needs `TEST_DATABASE_URL` set to a disposable database to run — do this before merging: `TEST_DATABASE_URL='host=127.0.0.1 user=... dbname=postgres sslmode=disable' go test ./server/api/ontology/assertions/ -run Integration -v`. Per project memory, never point it at `miner`.
- [x] 7.5 Updated the bug doc with a final status line for this round of fixes.
